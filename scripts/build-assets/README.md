# Build Assets

Builds generated project assets from their source schemas.

```sh
bun run build
```

The build currently generates:

- `apps/web/schemas/plugin-config/<manifest-version>.json`
- `apps/web/schemas/theme-config/<manifest-version>.json`
