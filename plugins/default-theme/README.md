# Recall Default Theme

Default theme plugin for the Recall app.

> Use this as a starter point when creating other theme plugins.

Recall treats this default theme as the fallback theme.

If another theme plugin has missing or invalid values, Recall will merge it
with this theme and use the default values where needed.

# Build

Use `build-recall-plugin` command from @recall/plugin-creator package to build this plugin.
